When the user is on the homepage, they can click the Login button. This will reveal a dropdown menu with two fields, one for Email and one for Password.

The user can type "knazario@bitovi.com" into the email field and "bit" into the password field, then click enter.